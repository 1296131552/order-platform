# 订单可视化平台 - 架构设计文档

> **后端系统架构设计与开发规范**

> 本文档定义了订单可视化平台的系统架构、模块设计、技术选型和开发规范，用于指导后端开发和架构演进。

---

## 📋 目录

- [项目概述](#项目概述)
- [技术架构](#技术架构)
- [系统架构](#系统架构)
- [模块设计](#模块设计)
- [技术选型](#技术选型)
- [开发规范](#开发规范)
- [修改规范](#修改规范)
- [更新记录](#更新记录)

---

## 项目概述

### 项目定位

**订单可视化数字化管理平台** - 以销售订单为核心，实现业务全流程可视化、资料统一管理、数据看板决策支持的业务管理系统。

### 核心价值

- 📊 **可视化**：订单全流程可视化追踪，地图展示发运路线
- 📁 **资料管理**：附件统一存储，支持标签分类和全文检索
- 📈 **数据看板**：KPI 指标实时展示，支持决策分析
- ⚠️ **异常管理**：异常处理闭环（上报→分配→处理→反馈）

### 技术特点

- 🏗️ **分层架构**：清晰分层，职责明确
- 🔄 **模块化设计**：业务模块独立，易于扩展
- ⚡ **多级缓存**：Caffeine + Redis，性能优化
- 🔍 **全文检索**：Elasticsearch 附件检索

---

## 技术架构

### 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│ 前端层（表现层）                                                     │
│  ┌─────────────────────┐  ┌───────────────────────────────────┐  │
│  │ order-platform-web  │  │ order-platform-screen             │  │
│  │ （Web管理端）        │  │ （地图可视化）                    │  │
│  │ Vue3 + Element Plus  │  │ Vue3 + 高德地图 + ECharts + DataV│ │
│  └─────────────────────┘  └───────────────────────────────────┘  │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ BI 工具（KPI 指标看板端）                                          │  │
│  │ DataEase / FineBI / Superset                                     │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────┬─────────────────────────────────┘
                                │ HTTP/HTTPS
┌───────────────────────────────▼─────────────────────────────────┐
│ 接入层（Nginx）                                                      │
│  静态资源服务 + 反向代理 + 负载均衡 + SSL终止                      │
└───────────────────────────────┬─────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────┐
│ 后端 - 统一入口层（API 模块）                                      │
│  Spring Boot 3.2.x (Java 21)                                     │
│  Spring MVC + 全局配置 + 过滤器/拦截器 + 异常处理                   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────┐
│ 后端 - 业务服务层（核心业务模块）                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ 订单模块  │  │ 发运模块  │  │ 看板模块  │  │ 异常模块  │       │
│  │ Controller│  │ Controller│  │ Controller│  │ Controller│       │
│  │ Service   │  │ Service   │  │ Service   │  │ Service   │       │
│  │ Mapper    │  │ Mapper    │  │ Mapper    │  │ Mapper    │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
│                                                                 │
│  ┌──────────┐  ┌─────────────────────────────┐  ┌──────────┐   │
│  │ 合作方模块 │  │ 可视化模块                    │  │ 附件模块  │   │
│  │ Controller│  │ Controller（调用其他模块）   │  │ Controller│   │
│  └──────────┘  └─────────────────────────────┘  └──────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 模块间调用方式：通过 Service 接口，不直接依赖模块           │   │
│  └─────────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────┐
│ 后端 - 基础支撑层（公共模块）                                         │
│  order-platform-common                                               │
│  • 权限管理（JWT、数据权限）                                       │
│  • 日志留痕（操作日志、审计日志）                                   │
│  • 工具类（日期、字符串、地址等）                                     │
│  • 统一响应（Result、PageResult）                                  │
│  • 异常定义（BusinessException）                                   │
└─────────────────────────────────────────────────────────────────┘
        │                            │                    │
        ▼                            ▼                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 数据持久层                                                           │
│  ┌─────────────────┐  ┌─────────────────────────────────────┐    │
│  │ MySQL 8.0+      │  │ 缓存层（Caffeine + Redis）             │    │
│  │ MyBatis Plus    │  │ • 热点数据缓存（5分钟）              │    │
│  │ Druid 连接池     │  │ • 本地缓存（减少 Redis 压力）         │    │
│  └─────────────────┘  └─────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ Elasticsearch 8.11+（附件全文检索）                              │   │
│  │ • 附件元数据索引（文件名、标签）                               │   │
│  │ • 文件内容检索（PDF、Word、Excel）                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 架构分层说明

| 层级 | 职责 | 模块/技术 |
|------|------|----------|
| **前端层** | 用户界面展示和交互 | Vue3、Element Plus、高德地图、BI 工具 |
| **接入层** | 流量接入和分发 | Nginx（静态资源、反向代理、负载均衡） |
| **统一入口层** | API 入口、全局配置 | Spring Boot、Security、拦截器 |
| **基础支撑层** | 通用能力提供 | 权限、日志、工具类、统一响应 |
| **业务服务层** | 核心业务逻辑 | 订单、发运、看板、异常等业务模块 |
| **数据持久层** | 数据存储和访问 | MySQL、Redis、Elasticsearch |

---

## 系统架构

### 模块划分

```
order-platform-backend/
├── order-platform-api/              # API 入口模块（主启动类）
├── order-platform-common/           # 公共模块（权限、日志、工具类）
├── order-platform-order/            # 订单模块（订单 CRUD、状态流转）
├── order-platform-shipment/         # 发运模块（含收货签收管理）
├── order-platform-partner/          # 合作方模块（供应商/承运商/客户）
├── order-platform-attachment/       # 附件模块（文件存储、全文检索）
├── order-platform-visualization/    # 可视化模块（地图路线、时间线）
├── order-platform-dashboard/         # 看板模块（数据口径统一、KPI 统计）
├── order-platform-exception/        # 异常处理模块（上报→分配→处理→反馈）
└── order-platform-user/             # 用户模块（用户、角色、权限）
```

### 模块依赖关系

```
                    ┌─────────────────┐
                    │  API 模块        │
                    └────────┬────────┘
                             │ 依赖
              ┌────────────┴────────────┐
              │     Common 模块          │
              └────────────┬────────────┘
                             │ 所有业务模块依赖
              ┌────────────┴────────────┐
              │                          │
    ┌─────────┴────────┬──────────────┐ │
    │                   │              │ │
┌───┴──┐ ┌──┴──┐ ┌────┴───┐ ┌────┴───┐ ┌─┴────┐
│订单│ │发运│ │合作方│ │可视化│ │看板│ │异常│ │附件│ │用户│
└───┬──┘ └───┘ └────┬──┘ └────┬──┘ └──┬──┘ └───┘
    │             │       │       │       │     │
    └─────────────┴───────┴───────┴─────┴─────┘
         通过 Service 接口调用，不直接依赖
```

---

## 模块设计

### 1. order-platform-common（公共模块）

**职责**：提供通用能力，被所有业务模块依赖

**核心功能**：
- 权限管理（JWT、数据权限）
- 日志留痕（操作日志、审计日志）
  - **操作日志**：采用混合存储方案
    - 核心信息（操作人、时间、结果）→ MySQL 主表（t_operation_log）
    - 详细快照（before/after）→ 对象存储（OSS/MinIO）
    - 全文检索（可选）→ Elasticsearch
- 工具类（日期、字符串、地址）
- 统一响应（Result、PageResult）
- 异常定义（BusinessException）

**关键类**：
```
common/
├── annotation/RequireLogin.java        # 登录认证注解
├── dto/CurrentUser.java               # 当前用户信息
├── enums/ResponseCode.java            # 响应码枚举
├── exception/BusinessException.java  # 业务异常
├── exception/GlobalExceptionHandler.java # 全局异常处理
├── holder/CurrentUserHolder.java      # ThreadLocal 用户上下文
├── interceptor/AuthInterceptor.java    # 认证拦截器
├── provider/UserRoleProvider.java     # 用户角色提供者接口
├── response/Result.java              # 统一响应封装
└── util/JwtUtil.java                 # JWT 工具类
```

**依赖规则**：
- ✅ 不依赖任何业务模块
- ✅ 可被所有业务模块依赖
- ✅ 保持轻量级，避免引入过多依赖

---

### 2. order-platform-order（订单模块）

**职责**：订单 CRUD、状态流转、订单行管理

**核心功能**：
- 订单创建、修改、删除、查询
- 订单状态流转（草稿→执行中→部分到货→完成）
- 订单行管理
- 订单统计

**Controller**：
- `OrderController`：订单管理接口
- `OrderLineController`：订单行管理接口

**Service**：
- `OrderService`：订单业务逻辑
- `OrderLineService`：订单行业务逻辑

**状态流转**：
```
DRAFT（草稿）
  ↓
EXECUTING（执行中）
  ↓
PARTIALLY_RECEIVED（部分到货）
  ↓
COMPLETED（已完成）
```

---

### 3. order-platform-shipment（发运模块）

**职责**：发运批次管理、**收货签收管理**

**核心功能**：
- **发运批次管理**：
  - 发运批次创建：按订单拆分多批次发运计划
  - 信息录入：提货/到货信息、物流单号、凭证照片
  - 状态更新：支持手动或自动同步发运进度
- **收货签收管理**：
  - 签收单上传：支持签收单文件上传，关联发运批次
  - 到货照片上传：支持到货照片批量上传
  - 差异记录：记录到货数量/质量差异，形成处理闭环
  - 签收确认：支持收货方在线确认或线下凭证上传确认
  - 签收状态管理：待签收→已签收→有差异→已处理

**Controller**：
- `ShipmentController`：发运批次管理接口
- `ReceiptController`：收货签收管理接口

**Service**：
- `ShipmentService`：发运服务
- `ReceiptService`：签收服务

**状态流转**：
```
PENDING（待提货）
  ↓
IN_TRANSIT（在途）
  ↓
DELIVERED（已到货）
```

---

### 4. order-platform-partner（合作方模块）

**职责**：供应商、承运商、客户管理

**核心功能**：
- 供应商：资质审核、合作管理、绩效评估
- 承运商：资质审核、运力管理、准时率统计
- 客户：信息管理、联系人管理

**Controller**：
- `SupplierController`：供应商管理接口
- `CarrierController`：承运商管理接口
- `CustomerController`：客户管理接口

---

### 5. order-platform-attachment（附件模块）

**职责**：文件存储、全文检索

**核心功能**：
- 附件上传/下载/删除
- 附件元数据管理
- 全文检索（Elasticsearch）
- 标签分类管理

**Controller**：
- `AttachmentController`：附件管理接口

**Service**：
- `FileStorageService`：文件存储服务（统一接口）
  - 支持本地存储（开发环境）
  - 支持阿里云 OSS（生产环境）
  - 支持 MinIO（自建对象存储）
- `AttachmentSearchService`：附件检索服务（ES）

---

### 6. order-platform-visualization（可视化模块）

**职责**：地图路线、时间线组装

**核心功能**：
- 地图路线数据组装
- 时间线数据组装
- 调用其他模块 Service 获取数据

**Controller**：
- `VisualizationController`：可视化数据接口

**Service**：
- `VisualizationService`：组装可视化数据
- `RouteService`：路线数据组装
- `TimelineService`：时间线数据组装

---

### 7. order-platform-dashboard（看板模块）

**职责**：看板数据组装、**数据口径统一**

**核心功能**：
- KPI 指标统计
- **数据口径统一机制**（KpiCalculateService）
- 趋势图数据组装
- 排行榜数据统计

**统一指标定义**：
- **订单数量**：按创建时间统计
- **在途订单**：发运已启动但未完成签收的订单
- **准时率**：按时签收订单数/总完成订单数×100%
- **异常件数**：存在到货差异或运输异常的订单数

**Controller**：
- `DashboardController`：看板数据接口

**Service**：
- `DashboardService`：看板数据组装
- `KpiCalculateService`：KPI 指标计算服务（统一口径）

---

### 8. order-platform-exception（异常处理模块）

**职责**：异常处理闭环（上报→分配→处理→反馈）

**核心功能**：
- 异常上报：支持运输延迟、到货差异、凭证缺失等异常类型上报
- 异常分配：自动或手动分配给责任人
- 异常处理：处理进度跟踪、处理结果记录
- 异常反馈：处理结果反馈给上报人，形成闭环
- 消息通知：邮件通知、站内消息

**Controller**：
- `ExceptionController`：异常管理接口

**Service**：
- `ExceptionReportService`：异常上报
- `ExceptionAssignService`：异常分配
- `ExceptionProcessService`：异常处理
- `ExceptionFeedbackService`：异常反馈

---

### 9. order-platform-user（用户模块）

**职责**：用户、角色、权限管理

**核心功能**：
- 用户管理（CRUD、状态管理）
- 角色管理（角色定义、权限分配）
- 用户角色关联
- 登录认证

**Controller**：
- `UserController`：用户管理接口
- `RoleController`：角色管理接口
- `AuthController`：认证接口

**Service**：
- `UserService`：用户服务
- `RoleService`：角色服务
- `UserRoleService`：用户角色关联服务

---

## 技术选型

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **JDK** | 21 | Java 运行环境（LTS 版本） |
| **Spring Boot** | 3.2.x | 核心框架 |
| **Spring MVC** | 内置 | Web 框架（RESTful API） |
| **Spring Security** | 6.x | 安全框架（认证授权） |
| **MyBatis Plus** | 3.5.x | ORM 框架（推荐使用注解形式） |
| **MySQL** | 8.0+ | 关系数据库 |
| **Druid** | 1.2.20+ | 数据库连接池 |
| **Redis** | 6.2+ / 7.0+ | 缓存/Session 存储 |
| **Caffeine** | 3.1+ | 本地缓存 |
| **Elasticsearch** | 8.11+ | 全文检索 |
| **JWT** | 0.12.3 | Token 认证 |
| **Knife4j** | 4.4.0+ | API 文档（OpenAPI 3.0） |
| **Lombok** | 最新 | 简化 Java 代码 |
| **Hutool** | 5.8.x | Java 工具类库 |
| **EasyExcel** | 3.3.2+ | Excel 导入导出 |

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue** | 3.3.4+ | 核心框架 |
| **Vite** | 4.4.5+ | 构建工具 |
| **Element Plus** | 2.4.2+ | UI 组件库 |
| **ECharts** | 5.4.3+ | 图表库 |
| **高德地图** | 最新 | 地图可视化 |
| **DataV** | 2.10+ | 大屏组件库（自研大屏） |

### BI 工具（KPI 看板）

| 工具 | 类型 | 用途 |
|------|------|------|
| **Apache Superset** | 开源 | 功能强大，社区活跃 |
| **DataEase** | 开源 | 国产，中文文档完善 |
| **Metabase** | 开源 | 简单易用，快速上线 |
| **FineBI** | 商业 | 专业级，大屏能力强 |

---

## 开发规范

### 代码规范

#### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| **类名** | 大驼峰（PascalCase） | `OrderService` |
| **方法名/变量名** | 小驼峰（camelCase） | `getOrderById` |
| **常量** | 全大写下划线分隔 | `MAX_RETRY_COUNT` |
| **包名** | 全小写 | `com.order.platform.order.service` |
| **数据库表名** | 小写+下划线，t_ 前缀 | `t_order` |
| **数据库字段名** | 小写+下划线分隔 | `order_no` |

#### 注释规范

**类级注释**：
```java
/**
 * 订单服务实现类
 *
 * 功能说明：
 * - 订单 CRUD 操作
 * - 订单状态流转
 * - 订单统计
 *
 * @author 开发组
 * @since 1.0.0
 */
public class OrderServiceImpl implements OrderService {
    // ...
}
```

**方法级注释**：
```java
/**
 * 根据订单号查询订单
 *
 * 业务规则：
 * - 订单号唯一性检查
 * - 逻辑删除的订单不返回
 * - 查询结果缓存 5 分钟
 *
 * @param orderNo 订单号
 * @return 订单实体，不存在返回 null
 */
public Order getByOrderNo(String orderNo) {
    // ...
}
```

#### 异常处理规范

- 使用 `BusinessException` 抛出业务异常
- 使用 `ResponseCode` 定义错误码
- 全局异常处理器统一处理

```java
// 抛出业务异常
throw new BusinessException(ResponseCode.ORDER_NOT_FOUND);

// 自定义消息
throw new BusinessException(ResponseCode.VALIDATION_ERROR, "订单号不能为空");
```

### 数据库规范

#### 表命名

- 使用 `t_` 前缀
- 小写+下划线分隔
- 示例：`t_order`、`t_shipment`

#### 字段命名

- 小写+下划线分隔
- 示例：`order_no`、`create_time`

#### 公共字段

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `id` | BIGINT | 主键 | 自增 |
| `create_time` | DATETIME | 创建时间 | CURRENT_TIMESTAMP |
| `update_time` | DATETIME | 更新时间 | CURRENT_TIMESTAMP ON UPDATE |
| `create_by` | BIGINT | 创建人ID | -1 |
| `update_by` | BIGINT | 更新人ID | -1 |
| `is_deleted` | TINYINT | 逻辑删除 | 0 |

#### MyBatis-Plus 使用规范

> **核心原则**：简单 CRUD 用 MyBatis-Plus 方法，复杂查询才手写 SQL

**使用场景划分**：

| 场景 | 使用方式 | 示例 |
|------|---------|------|
| **单表 CRUD** | BaseMapper 方法 | `insert()`、`deleteById()`、`updateById()`、`selectById()`、`selectList()` |
| **条件查询** | QueryWrapper/LambdaQueryWrapper | `wrapper.eq("status", 1).like("name", "张")` |
| **分页查询** | MyBatis-Plus 分页插件 | `Page<T> page = new Page<>(1, 10)` |
| **复杂多表联查** | 手写 SQL（注解/XML） | `@Select("SELECT * FROM t_order ...")` |
| **动态 SQL** | 手写 SQL（@SelectProvider） | 复杂条件拼接 |

**推荐做法**：

```java
// ✅ 推荐：简单 CRUD 使用 BaseMapper
@Mapper
public interface OrderMapper extends BaseMapper<Order> {
    // 无需手写 SQL，自动继承增删改查方法
}

// Service 中使用
@Service
public class OrderService {
    @Autowired
    private OrderMapper orderMapper;

    // 插入
    orderMapper.insert(order);

    // 根据 ID 查询
    Order order = orderMapper.selectById(orderId);

    // 条件查询
    List<Order> orders = orderMapper.selectList(
        new LambdaQueryWrapper<Order>()
            .eq(Order::getCustomerId, customerId)
            .eq(Order::getIsDeleted, 0)
            .orderByDesc(Order::getCreatedAt)
    );

    // 分页查询
    Page<Order> page = new Page<>(1, 10);
    Page<Order> result = orderMapper.selectPage(page, wrapper);
}
```

**手写 SQL 场景**：

```java
// ✅ 复杂多表联查，手写 SQL
@Mapper
public interface OrderMapper extends BaseMapper<Order> {

    @Select("SELECT o.*, c.name as customer_name " +
            "FROM t_order o " +
            "LEFT JOIN t_customer c ON o.customer_id = c.id " +
            "WHERE o.is_deleted = 0 " +
            "AND c.status = 'ACTIVE' " +
            "ORDER BY o.created_at DESC")
    List<OrderVO> selectOrdersWithCustomer();
}
```

**性能注意事项**：

| 问题 | 说明 | 解决方案 |
|------|------|----------|
| **N+1 查询** | 循环查询关联数据 | 使用 `@TableField(select = false)` + 手写 SQL 联查 |
| **全字段查询** | 不需要的字段也查出来 | 使用 `select(...)` 指定字段 |
| **深分页** | `LIMIT 1000000, 10` 性能差 | 使用 ID 范围查询或搜索引擎 |

### API 规范

#### RESTful 风格

| HTTP 方法 | 用途 | 示例 |
|-----------|------|------|
| **GET** | 查询 | `GET /api/order/{id}` |
| **POST** | 新增 | `POST /api/order` |
| **PUT** | 全量更新 | `PUT /api/order/{id}` |
| **PATCH** | 部分更新 | `PATCH /api/order/{id}/status` |
| **DELETE** | 删除 | `DELETE /api/order/{id}` |

#### URL 设计

```
/api/{模块}/{资源}/{操作}
```

**示例**：
- `/api/order/list` - 订单列表
- `/api/order/{id}` - 订单详情
- `/api/order/{id}/cancel` - 取消订单

#### 统一响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {...},
  "timestamp": "2026-01-07T12:00:00"
}
```

---

## 修改规范

> **重要**：架构是系统的基础，修改时请严格遵守以下规范。

### 📝 修改前检查清单

- [ ] 确认修改是否影响其他模块
- [ ] 确认是否需要更新数据库设计
- [ ] 确认是否需要更新技术选型
- [ ] 确认是否需要更新本文档
- [ ] 评估性能影响
- [ ] 更新"更新记录"

### ✅ 允许的修改

| 修改类型 | 说明 | 示例 |
|---------|------|------|
| **新增模块** | 添加新的业务模块 | 新增报表模块 |
| **新增功能** | 在现有模块中添加功能 | 订单模块新增批量导入 |
| **技术升级** | 升级技术版本 | Spring Boot 3.2.x → 3.3.x |
| **Bug 修复** | 修复架构问题 | 修复循环依赖 |
| **性能优化** | 优化架构性能 | 引入缓存机制 |
| **文档更新** | 更新架构文档 | 同步最新架构设计 |

### ❌ 禁止的修改

| 修改类型 | 原因 | 替代方案 |
|---------|------|----------|
| **删除模块** | 会破坏系统功能 | 标记为 `@Deprecated` |
| **修改模块职责** | 会导致职责不清 | 新增子模块 |
| **循环依赖** | 会导致编译失败 | 通过接口解耦 |
| **破坏分层** | 会导致架构混乱 | 严格遵守分层原则 |
| **修改公共模块** | 影响所有业务模块 | 谨慎评估，确保向后兼容 |

### 🔄 架构演进流程

1. **需求分析**
   - 明确架构需求
   - 评估影响范围
   - 制定演进方案

2. **设计评审**
   - 技术评审
   - 架构评审
   - 风险评估

3. **方案制定**
   - 制定详细方案
   - 制定迁移计划
   - 制定回滚方案

4. **实施验证**
   - 灰度部署
   - 功能验证
   - 性能测试

5. **文档更新**
   - 更新架构文档
   - 更新相关文档
   - 通知团队成员

### 📋 架构修改模板

修改架构时，请按以下模板更新文档：

```markdown
### 架构修改标题

**修改日期**：YYYY-MM-DD
**修改人**：你的名字
**修改类型**：新增模块 / 技术升级 / 功能优化 / Bug 修复

**修改说明**：
- 修改背景和原因
- 修改内容和范围
- 影响评估

**架构图变更**：
```diff
- 原架构内容
+ 新架构内容
```

**模块变更**：
| 模块 | 变更类型 | 说明 |
|------|----------|------|
| order-platform-order | 新增功能 | 新增批量导入功能 |

**技术栈变更**：
| 技术 | 旧版本 | 新版本 | 原因 |
|------|--------|--------|------|
| Spring Boot | 3.2.0 | 3.3.0 | 新特性支持 |

**影响范围**：
- 影响的模块
- 需要修改的配置
- 需要更新的文档

**迁移计划**：
1. 步骤一
2. 步骤二
3. 步骤三

**回滚方案**：
- 回滚条件
- 回滚步骤

**相关 Issue/PR**：#123
```

---

## 更新记录

### v1.0.2 (2026-01-07)

#### 更新 common 模块日志留痕说明

**修改人**：开发组

**修改内容**：
- 在"模块设计"章节更新 common 模块的"日志留痕"功能说明
- 明确操作日志采用混合存储方案
- 说明核心信息、详细快照、全文检索的存储方式

**相关文件**：
- 系统架构设计.md（已更新）
- 数据库设计文档.md（已更新操作日志表设计）

---

### v1.0.1 (2026-01-07)

#### 新增 MyBatis-Plus 使用规范

**修改人**：开发组

**修改内容**：
- 在"数据库规范"章节新增"MyBatis-Plus 使用规范"小节
- 明确核心原则：简单 CRUD 用 MyBatis-Plus 方法，复杂查询才手写 SQL
- 添加使用场景划分表（单表 CRUD、条件查询、分页查询、复杂多表联查、动态 SQL）
- 提供推荐做法和手写 SQL 场景示例代码
- 添加性能注意事项（N+1 查询、全字段查询、深分页）

**相关文件**：
- 系统架构设计.md（已更新）

---

### v1.0.0 (2026-01-07)

#### 初始架构设计

**模块划分**：
- ✅ order-platform-api（API 入口模块）
- ✅ order-platform-common（公共模块）
- ✅ order-platform-order（订单模块）
- ✅ order-platform-shipment（发运模块，含签收管理）
- ✅ order-platform-partner（合作方模块）
- ✅ order-platform-attachment（附件模块）
- ✅ order-platform-visualization（可视化模块）
- ✅ order-platform-dashboard（看板模块，数据口径统一）
- ✅ order-platform-exception（异常处理模块）
- ✅ order-platform-user（用户模块）

**技术栈确定**：
- ✅ Spring Boot 3.2.x + Java 21
- ✅ MyBatis Plus（注解形式）
- ✅ Redis + Caffeine（多级缓存）
- ✅ Elasticsearch（全文检索）
- ✅ Knife4j（API 文档）
- ✅ JWT（认证授权）
- ✅ Vue 3 + Element Plus（前端）
- ✅ 混合大屏方案（自研地图 + BI 工具）

**架构特点**：
- ✅ 分层架构，职责清晰
- ✅ 模块化设计，独立开发
- ✅ 接口解耦，避免循环依赖
- ✅ 多级缓存，性能优化
- ✅ 数据口径统一，避免偏差

**关键设计决策**：
- ✅ 签收管理归属 shipment 模块
- ✅ KPI 口径统一（KpiCalculateService）
- ✅ 地址工具类（AddressUtil）
- ✅ 文件存储统一接口（FileStorageService）
- ✅ 定时任务推荐 XXL-Job

**待完善功能**：
- ⏳ 缓存预热机制
- ⏳ 异步处理优化
- ⏳ 状态机集成（可选）
- ⏳ 工作流引擎（可选）

---

## 附录

### A. 相关文档

- [数据库设计文档](../../docs/数据库/0.数据库设计文档.md)
- [架构评估报告](../../docs/技术栈和架构/架构评估报告.md)
- [文件布局设计](../../docs/技术栈和架构/文件布局设计.md)
- [项目计划](../../docs/项目计划.md)
- [API 接口文档](./API接口文档.md)

### B. 技术架构对比

| 方案 | 优势 | 劣势 | 适用场景 |
|------|------|------|----------|
| **单体架构** | 简单易维护、部署方便 | 扩展性受限 | 当前方案 |
| **微服务架构** | 松耦合、易扩展 | 复杂度高、运维成本高 | 大规模场景 |
| **混合架构** | 兼顾简单和扩展 | 折中方案 | 中大型项目 |

### C. 性能指标

| 指标 | 目标值 | 优化方案 |
|------|--------|----------|
| **接口响应时间** | ≤3 秒 | 多级缓存、异步处理 |
| **并发支持** | 1000+ QPS | Redis 缓存、连接池优化 |
| **数据库查询** | ≤100ms | 索引优化、分页优化 |
| **附件检索** | ≤2 秒 | Elasticsearch 索引优化 |

### D. 安全设计

| 安全措施 | 实现方式 |
|---------|----------|
| **认证** | JWT Token + Spring Security |
| **授权** | RBAC 角色权限控制 |
| **加密** | 敏感信息加密存储（环境变量/Jasypt） |
| **防护** | SQL 注入防护、XSS 防护 |
| **审计** | 操作日志、审计日志 |

---

## 维护者

- **开发组** - 架构设计与文档维护

---

## 许可证

本项目采用 MIT 许可证。

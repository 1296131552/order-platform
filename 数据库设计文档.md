# 订单可视化平台 - 数据库设计文档

> **后端开发指南 - 数据库设计与开发规范**

> 本文档定义了订单可视化平台的数据库架构、表结构设计、索引策略和开发规范，用于指导后端数据库开发和维护。

> **🔔 重要提示**：本文档中的数据库表设计可以根据实际业务需求进行调整。在不修改数据库表结构（如字段类型、主键、外键等）的前提下，可以根据业务需要：
> - 新增业务表
> - 新增字段到现有表
> - 新增索引优化查询
> - 调整字段默认值
> - 调整字段注释说明
>
> **⚠️ 任何修改后必须更新本文档的"更新记录"部分！**

---

## 📋 目录

- [数据库概述](#数据库概述)
- [技术规格](#技术规格)
- [数据库架构](#数据库架构)
- [表结构设计](#表结构设计)
- [索引设计](#索引设计)
- [开发规范](#开发规范)
- [修改规范](#修改规范)
- [更新记录](#更新记录)

---

## 数据库概述

### 数据库定位

**订单可视化数字化管理平台数据库** - 以订单为核心的全流程业务数据存储，支持订单管理、发运跟踪、签收管理、异常处理等业务场景。

### 核心特性

- 📊 **关系型设计**：基于MySQL 8.0的关系型数据库
- 🔄 **状态机管理**：统一的状态字典和状态流转规则
- 🏷️ **多态关联**：灵活的业务对象关联设计
- 📝 **软删除机制**：数据可恢复，支持审计追溯
- 🔍 **全文检索**：Elasticsearch附件内容检索

---

## 技术规格

| 规格 | 说明 |
|------|------|
| **数据库类型** | MySQL 8.0+ |
| **字符集** | utf8mb4 |
| **排序规则** | utf8mb4_unicode_ci |
| **存储引擎** | InnoDB |
| **表数量** | 27张表 |
| **设计规范** | 遵循3NF，适度反范式 |

---

## 数据库架构

### 模块划分

| 模块 | 表数量 | 表列表 |
|------|--------|--------|
| **订单模块** | 4 | t_order, t_order_line, t_order_status_log, t_order_line_status_log |
| **发运模块** | 4 | t_shipment, t_shipment_line, t_shipment_status_log, t_shipment_line_status_log |
| **签收模块** | 1 | t_receipt_detail |
| **合作方模块** | 4 | t_customer, t_supplier, t_carrier, t_partner_performance |
| **异常管理** | 3 | t_exception, t_exception_handling, t_exception_type |
| **附件管理** | 3 | t_attachment, t_attachment_tag, attachment_tag_relation |
| **状态管理** | 2 | t_status_dict, t_status_transition_rule |
| **用户权限** | 5 | t_user, t_role, t_permission, t_user_role, t_role_permission |
| **系统支撑** | 3 | t_operation_log, t_data_dict, t_system_config |

### 表关系图

```
Customer (客户)
    │
    │ 1:N
    ▼
Order (订单)
    │
    │ 1:N
    ▼
OrderLine (订单行) ──── N:1 ──── Supplier (供应商)
    │
    │ 1:N
    ▼
Shipment (发运计划)
    │
    │ 1:N
    ▼
ShipmentLine (快递单) ──── N:1 ──── Carrier (承运商)
    │
    │ 1:1
    ▼
ReceiptDetail (签收明细)
```

---

## 表结构设计

### 公共字段规范

每张业务表应包含以下公共字段：

```sql
-- 主键
`id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',

-- 创建信息
`created_at` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '创建时间',
`created_by` BIGINT NOT NULL DEFAULT 0 COMMENT '创建人ID:0-系统创建',

-- 更新信息
`updated_at` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3) COMMENT '更新时间',
`updated_by` BIGINT NOT NULL DEFAULT 0 COMMENT '更新人ID:0-系统创建',

-- 逻辑删除
`is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除:0-未删除,1-已删除'
```

### 1. 订单表（t_order）

**表说明**：销售订单主表，记录订单基本信息、状态、客户关联等核心数据

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| order_no | VARCHAR(50) | 是 | - | 订单号（唯一） |
| customer_id | BIGINT | 是 | - | 客户ID（外键） |
| status_dict_id | BIGINT | 否 | NULL | 状态字典ID |
| status_code | VARCHAR(30) | 否 | '' | 订单状态代码 |
| total_amount | DECIMAL(18,2) | 是 | 0.00 | 订单总金额 |
| order_date | DATE | 是 | - | 订单日期 |
| delivery_address | VARCHAR(500) | 否 | '' | 送货地址 |
| contact_name | VARCHAR(50) | 否 | '' | 联系人姓名 |
| contact_phone | VARCHAR(20) | 否 | '' | 联系人电话 |
| remark | TEXT | 否 | NULL | 备注 |
| extra_info | JSON | 否 | NULL | 扩展信息（JSON格式） |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_order_order_no` (`order_no`, `is_deleted`),
KEY `idx_order_customer_status` (`customer_id`, `status_code`, `is_deleted`),
KEY `idx_order_date_status` (`order_date`, `status_code`, `is_deleted`)
```

**状态流转**：
```
DRAFT（草稿）→ CONFIRMED（已确认）→ EXECUTING（执行中）→
PARTIALLY_RECEIVED（部分到货）→ COMPLETED（已完成）
                    ↓
                CANCELLED（已取消）
```

---

### 2. 订单行表（t_order_line）

**表说明**：订单明细表，记录订单行的产品信息、数量、金额、供应商关联、收货地址等

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| order_id | BIGINT | 是 | - | 订单ID（外键） |
| line_no | INT | 是 | - | 行号 |
| supplier_id | BIGINT | 是 | - | 供应商ID（外键） |
| product_code | VARCHAR(50) | 是 | - | 产品编码 |
| product_name | VARCHAR(200) | 是 | - | 产品名称 |
| quantity | DECIMAL(18,2) | 是 | - | 数量 |
| unit | VARCHAR(20) | 否 | '' | 单位 |
| unit_price | DECIMAL(18,2) | 是 | 0.00 | 单价 |
| total_amount | DECIMAL(18,2) | 是 | 0.00 | 金额 |
| delivery_address | VARCHAR(500) | 否 | '' | 送货地址 |
| status_dict_id | BIGINT | 否 | NULL | 状态字典ID |
| status_code | VARCHAR(30) | 否 | '' | 订单行状态代码 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_order_line_order_line_no` (`order_id`, `line_no`, `is_deleted`),
KEY `idx_order_line_order_status` (`order_id`, `status_code`, `is_deleted`),
KEY `idx_order_line_supplier` (`supplier_id`, `is_deleted`)
```

---

### 3. 发运计划表（t_shipment）

**表说明**：发运计划主表，运营人员设计的发运计划，审核通过后便可以发货

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| shipment_no | VARCHAR(50) | 是 | - | 发运计划号（唯一） |
| order_id | BIGINT | 是 | - | 订单ID（外键） |
| status_dict_id | BIGINT | 否 | NULL | 状态字典ID |
| status_code | VARCHAR(30) | 否 | '' | 发运计划状态代码 |
| shipment_date | DATE | 否 | NULL | 发运日期 |
| expected_delivery_date | DATE | 否 | NULL | 要求到货日期 |
| total_quantity | DECIMAL(18,2) | 是 | 0 | 总数量 |
| total_amount | DECIMAL(18,2) | 是 | 0.00 | 总金额 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_shipment_shipment_no` (`shipment_no`, `is_deleted`),
KEY `idx_shipment_order` (`order_id`, `is_deleted`),
KEY `idx_shipment_status` (`status_code`, `is_deleted`)
```

**状态流转**：
```
DRAFT（草稿）→ APPROVED（已审核）→ EXECUTING（执行中）→ COMPLETED（已完成）
```

---

### 4. 快递单表（t_shipment_line）

**表说明**：快递单表，包含承运商、收货方、物流单号等具体信息

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| shipment_id | BIGINT | 是 | - | 发运计划ID（外键） |
| order_line_id | BIGINT | 是 | - | 订单行ID（外键） |
| line_no | INT | 是 | - | 行号 |
| carrier_id | BIGINT | 是 | - | 承运商ID（外键） |
| tracking_no | VARCHAR(50) | 是 | - | 物流单号 |
| pickup_address | VARCHAR(500) | 否 | '' | 取货地址 |
| delivery_address | VARCHAR(500) | 是 | - | 收货地址 |
| contact_name | VARCHAR(50) | 是 | - | 联系人姓名 |
| contact_phone | VARCHAR(20) | 是 | - | 联系人电话 |
| quantity | DECIMAL(18,2) | 是 | - | 数量 |
| status_dict_id | BIGINT | 否 | NULL | 状态字典ID |
| status_code | VARCHAR(30) | 否 | '' | 快递单状态代码 |
| pickup_time | DATETIME(3) | 否 | NULL | 取货时间 |
| delivery_time | DATETIME(3) | 否 | NULL | 送达时间 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_shipment_line_shipment_line_no` (`shipment_id`, `line_no`, `is_deleted`),
KEY `idx_shipment_line_order_line` (`order_line_id`, `is_deleted`),
KEY `idx_shipment_line_carrier` (`carrier_id`, `is_deleted`),
KEY `idx_shipment_line_tracking` (`tracking_no`, `is_deleted`)
```

**状态流转**：
```
CREATED（已创建）→ PICKED_UP（已取货）→ IN_TRANSIT（运输中）→ DELIVERED（已送达）
```

---

### 5. 签收明细表（t_receipt_detail）

**表说明**：签收明细表，记录具体签收数量，直接关联快递单，包含差异信息

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| shipment_line_id | BIGINT | 是 | - | 快递单ID（外键，唯一） |
| expected_quantity | DECIMAL(18,2) | 是 | - | 期望数量 |
| received_quantity | DECIMAL(18,2) | 是 | - | 签收数量 |
| difference_quantity | DECIMAL(18,2) | 是 | 0 | 差异数量 |
| receipt_status | VARCHAR(30) | 是 | 'RECEIVED' | 签收状态 |
| receipt_date | DATETIME(3) | 是 | - | 签收日期 |
| receiver_name | VARCHAR(50) | 是 | - | 收货人姓名 |
| receiver_phone | VARCHAR(20) | 是 | - | 收货人电话 |
| difference_description | TEXT | 否 | NULL | 差异说明 |
| receipt_photos | JSON | 否 | NULL | 签收照片（JSON数组） |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_receipt_detail_shipment_line` (`shipment_line_id`, `is_deleted`),
KEY `idx_receipt_detail_date` (`receipt_date`, `is_deleted`),
KEY `idx_receipt_detail_status` (`receipt_status`, `is_deleted`)
```

**签收状态**：
- `RECEIVED`：已签收（无差异）
- `DIFFERENCE`：有差异
- `PROCESSED`：已处理

---

### 6. 客户表（t_customer）

**表说明**：客户信息表，记录客户基本信息、联系方式、地址等

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| customer_code | VARCHAR(50) | 是 | - | 客户编码（唯一） |
| customer_name | VARCHAR(200) | 是 | - | 客户名称 |
| contact_name | VARCHAR(50) | 否 | '' | 联系人姓名 |
| contact_phone | VARCHAR(20) | 否 | '' | 联系人电话 |
| contact_email | VARCHAR(100) | 否 | '' | 联系人邮箱 |
| address_province | VARCHAR(50) | 否 | '' | 省份 |
| address_city | VARCHAR(50) | 否 | '' | 城市 |
| address_district | VARCHAR(50) | 否 | '' | 区县 |
| address_detail | VARCHAR(500) | 否 | '' | 详细地址 |
| is_enabled | TINYINT | 是 | 1 | 是否启用（0-否，1-是） |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_customer_code` (`customer_code`, `is_deleted`),
KEY `idx_customer_name` (`customer_name`, `is_deleted`)
```

---

### 7. 供应商表（t_supplier）

**表说明**：供应商信息表，记录供应商基本信息、联系方式、地址等

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| supplier_code | VARCHAR(50) | 是 | - | 供应商编码（唯一） |
| supplier_name | VARCHAR(200) | 是 | - | 供应商名称 |
| contact_name | VARCHAR(50) | 否 | '' | 联系人姓名 |
| contact_phone | VARCHAR(20) | 否 | '' | 联系人电话 |
| contact_email | VARCHAR(100) | 否 | '' | 联系人邮箱 |
| address_province | VARCHAR(50) | 否 | '' | 省份 |
| address_city | VARCHAR(50) | 否 | '' | 城市 |
| address_detail | VARCHAR(500) | 否 | '' | 详细地址 |
| business_license | VARCHAR(100) | 否 | '' | 营业执照号 |
| is_enabled | TINYINT | 是 | 1 | 是否启用 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_supplier_code` (`supplier_code`, `is_deleted`),
KEY `idx_supplier_name` (`supplier_name`, `is_deleted`)
```

---

### 8. 承运商表（t_carrier）

**表说明**：承运商信息表，记录承运商基本信息、联系方式、地址等

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| carrier_code | VARCHAR(50) | 是 | - | 承运商编码（唯一） |
| carrier_name | VARCHAR(200) | 是 | - | 承运商名称 |
| contact_name | VARCHAR(50) | 否 | '' | 联系人姓名 |
| contact_phone | VARCHAR(20) | 否 | '' | 联系人电话 |
| contact_email | VARCHAR(100) | 否 | '' | 联系人邮箱 |
| service_type | VARCHAR(100) | 否 | '' | 服务类型 |
| is_enabled | TINYINT | 是 | 1 | 是否启用 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_carrier_code` (`carrier_code`, `is_deleted`),
KEY `idx_carrier_name` (`carrier_name`, `is_deleted`)
```

---

### 9. 状态字典表（t_status_dict）

**表说明**：统一管理所有业务实体的状态定义，支持状态机管理

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| status_code | VARCHAR(50) | 是 | - | 状态代码 |
| status_name | VARCHAR(100) | 是 | - | 状态名称 |
| business_type | VARCHAR(50) | 是 | - | 业务类型 |
| status_category | VARCHAR(50) | 否 | '' | 状态分类 |
| is_final | TINYINT | 是 | 0 | 是否终态（0-否，1-是） |
| sort_order | INT | 是 | 0 | 排序号 |
| description | VARCHAR(500) | 否 | '' | 状态描述 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_status_code_business_type` (`status_code`, `business_type`, `is_deleted`),
KEY `idx_status_dict_business_type` (`business_type`, `is_deleted`)
```

---

### 10. 用户表（t_user）

**表说明**：系统用户表，记录用户基本信息、账号信息、组织信息、安全状态等

**核心字段**（25个）：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| **id** | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| **username** | VARCHAR(50) | 是 | - | 用户名（唯一） |
| **password** | VARCHAR(255) | 是 | - | 密码（BCrypt加密） |
| **user_code** | VARCHAR(50) | 是 | '' | 用户编号（业务唯一标识） |
| **real_name** | VARCHAR(50) | 是 | '' | 真实姓名 |
| **email** | VARCHAR(100) | 是 | '' | 邮箱 |
| **phone** | VARCHAR(20) | 是 | '' | 手机号 |
| **avatar** | VARCHAR(500) | 否 | NULL | 头像URL |
| **is_enabled** | TINYINT | 是 | 1 | 是否启用：0-禁用，1-启用 |
| **is_locked** | TINYINT | 是 | 0 | 是否锁定：0-未锁定，1-已锁定 |
| **locked_time** | DATETIME(3) | 否 | NULL | 锁定时间 |
| **locked_reason** | VARCHAR(500) | 是 | '' | 锁定原因 |
| **last_login_time** | DATETIME(3) | 否 | NULL | 最后登录时间 |
| **last_login_ip** | VARCHAR(50) | 是 | '' | 最后登录IP |
| **login_count** | INT | 是 | 0 | 登录次数 |
| **password_changed_time** | DATETIME(3) | 否 | NULL | 密码修改时间 |
| **password_expire_time** | DATETIME(3) | 否 | NULL | 密码过期时间 |
| **department_id** | BIGINT | 是 | -1 | 部门ID（-1表示未分配） |
| **department_name** | VARCHAR(100) | 是 | '' | 部门名称（冗余） |
| **position** | VARCHAR(50) | 是 | '' | 职位 |
| **employee_no** | VARCHAR(50) | 是 | '' | 工号 |
| **remark** | VARCHAR(500) | 是 | '' | 用户备注 |
| **created_at** | DATETIME(3) | 是 | CURRENT_TIMESTAMP(3) | 创建时间 |
| **created_by** | BIGINT | 是 | -1 | 创建人ID |
| **updated_at** | DATETIME(3) | 是 | CURRENT_TIMESTAMP(3) | 更新时间 |
| **updated_by** | BIGINT | 是 | -1 | 更新人ID |
| **is_deleted** | TINYINT | 是 | 0 | 是否删除：0-未删除，1-已删除 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_user_username` (`username`, `is_deleted`),
KEY `idx_user_enabled_locked` (`is_enabled`, `is_locked`, `is_deleted`),
KEY `idx_user_department` (`department_id`, `is_enabled`, `is_deleted`)
```

**字段说明**：
- **user_code**：用户编号，与 username 区分，用于业务对接（如 USER001）
- **locked_time / locked_reason**：安全审计，记录账户锁定详情
- **login_count**：用户行为统计，活跃用户分析
- **password_expire_time**：密码策略，强制定期修改密码
- **department_id / department_name**：数据权限隔离基础
- **employee_no**：企业系统必备的工号

---

### 11. 角色表（t_role）⭐ 新增

**表说明**：系统角色表，定义角色信息，用于权限管理和数据权限控制

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| **id** | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| **role_code** | VARCHAR(50) | 是 | - | 角色代码（唯一） |
| **role_name** | VARCHAR(50) | 是 | - | 角色名称 |
| **role_type** | VARCHAR(30) | 是 | 'BUSINESS' | 角色类型：BUSINESS/SYSTEM |
| **data_scope_type** | TINYINT | 是 | 3 | 数据权限：1-全部，2-本部门，3-本人，4-自定义 |
| **description** | VARCHAR(500) | 是 | '' | 角色描述 |
| **sort_order** | INT | 是 | 0 | 排序号 |
| **is_enabled** | TINYINT | 是 | 1 | 是否启用：0-禁用，1-启用 |
| **is_system** | TINYINT | 是 | 0 | 是否系统角色：0-用户自定义，1-系统内置 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_role_code` (`role_code`, `is_deleted`),
KEY `idx_role_type_enabled` (`role_type`, `is_enabled`, `is_deleted`)
```

**字段说明**：
- **role_code**：角色代码，如 `CUSTOMER_MANAGER`、`SYSTEM_ADMIN`
- **role_type**：`BUSINESS`（业务角色）vs `SYSTEM`（系统角色）
- **data_scope_type**：数据权限类型 ⭐ **核心字段**
  - 1 = ALL（全部数据）
  - 2 = DEPARTMENT（本部门数据）
  - 3 = SELF（本人数据）
  - 4 = CUSTOM（自定义范围）
- **is_system**：是否系统角色（系统角色不可删除）

**预定义角色**（5个）：

| role_code | role_name | role_type | data_scope_type | 说明 |
|-----------|-----------|-----------|-----------------|------|
| SYSTEM_ADMIN | 系统管理员 | SYSTEM | 1（全部） | 负责权限配置、系统管理 |
| DATA_ADMIN | 数据管理员 | SYSTEM | 1（全部） | 负责数据查看、导出 |
| CUSTOMER_MANAGER | 客户经理 | BUSINESS | 3（本人） | 负责订单创建与跟进 |
| PURCHASE_SPECIALIST | 采购专员 | BUSINESS | 3（本人） | 负责供应商选择、资质审核 |
| OPERATION_SPECIALIST | 运营专员 | BUSINESS | 3（本人） | 负责发运计划、在途跟踪 |

---

### 12. 用户角色关联表（t_user_role）⭐ 新增

**表说明**：用户角色关联中间表，实现用户与角色的N:M关联

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| **id** | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| **user_id** | BIGINT | 是 | - | 用户ID（外键） |
| **username** | VARCHAR(50) | 是 | '' | 用户名（冗余字段） |
| **role_id** | BIGINT | 是 | - | 角色ID（外键） |
| **role_code** | VARCHAR(50) | 是 | '' | 角色代码（冗余字段） |
| **is_primary** | TINYINT | 是 | 0 | 是否主角色：0-否，1-是 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_user_role` (`user_id`, `role_id`, `is_deleted`),
KEY `idx_user_role_user` (`user_id`, `is_deleted`),
KEY `idx_user_role_role` (`role_id`, `is_deleted`),
KEY `idx_user_role_primary` (`user_id`, `is_primary`, `is_deleted`)
```

**字段说明**：
- **role_code**：冗余字段，`UserRoleService.getRoleCodesByUserId()` 直接查询此字段
- **is_primary**：主角色标识 ⭐
  - 用户可能有多个角色
  - 数据权限以主角色的 `data_scope_type` 为准

---

### 13. 角色权限关联表（t_role_permission）⭐ 新增

**表说明**：角色权限关联中间表，实现角色与权限的N:M关联

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| **id** | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| **role_id** | BIGINT | 是 | - | 角色ID（外键） |
| **role_code** | VARCHAR(50) | 是 | '' | 角色代码（冗余字段） |
| **permission_code** | VARCHAR(100) | 是 | - | 权限代码 |

**索引**：

```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_role_permission` (`role_id`, `permission_code`, `is_deleted`),
KEY `idx_role_permission_role` (`role_id`, `is_deleted`)
```

**字段说明**：
- **permission_code**：权限代码，格式为 `{模块}:{操作}`
  - 示例：`ORDER:VIEW`、`SHIPMENT:CREATE`、`PARTNER:AUDIT`
- **不使用 `t_permission` 表**：权限是代码层面的硬编码，不需要动态管理

**权限代码规范**：

| 模块 | 权限代码示例 | 说明 |
|------|-------------|------|
| USER | `USER:*`、`USER:VIEW`、`USER:CREATE` | 用户管理 |
| ROLE | `ROLE:*`、`ROLE:VIEW`、`ROLE:CREATE` | 角色管理 |
| ORDER | `ORDER:*`、`ORDER:VIEW`、`ORDER:CREATE` | 订单管理 |
| PARTNER | `PARTNER:*`、`PARTNER:VIEW`、`PARTNER:AUDIT` | 合作方管理 |
| SHIPMENT | `SHIPMENT:*`、`SHIPMENT:VIEW`、`SHIPMENT:CREATE` | 发运管理 |
| ATTACHMENT | `ATTACHMENT:*`、`ATTACHMENT:VIEW`、`ATTACHMENT:UPLOAD` | 附件管理 |
| EXCEPTION | `EXCEPTION:*`、`EXCEPTION:VIEW`、`EXCEPTION:HANDLE` | 异常管理 |
| DASHBOARD | `DASHBOARD:*`、`DASHBOARD:VIEW` | 看板管理 |
| DATA | `DATA:*`、`DATA:EXPORT`、`DATA:IMPORT` | 数据管理 |

---

### 14. 附件表（t_attachment）

**表说明**：附件表，凭证与照片，通过多态关联支持关联多个业务实体

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| business_type | VARCHAR(50) | 是 | - | 业务类型（多态关联） |
| business_id | BIGINT | 是 | - | 业务ID（多态关联） |
| file_name | VARCHAR(200) | 是 | - | 文件名 |
| file_path | VARCHAR(500) | 是 | - | 文件路径 |
| file_size | BIGINT | 是 | 0 | 文件大小（字节） |
| file_type | VARCHAR(50) | 是 | - | 文件类型 |
| mime_type | VARCHAR(100) | 否 | '' | MIME类型 |
| file_md5 | VARCHAR(32) | 否 | '' | 文件MD5 |
| uploader_id | BIGINT | 是 | 0 | 上传人ID |
| upload_time | DATETIME(3) | 是 | CURRENT_TIMESTAMP(3) | 上传时间 |

**索引**：

```sql
PRIMARY KEY (`id`),
KEY `idx_attachment_business` (`business_type`, `business_id`, `is_deleted`),
KEY `idx_attachment_uploader` (`uploader_id`, `is_deleted`)
```

**业务类型（business_type）**：
- `order`：订单
- `order_line`：订单行
- `shipment`：发运计划
- `shipment_line`：快递单
- `receipt_detail`：签收明细
- `supplier`：供应商
- `carrier`：承运商
- `customer`：客户
- `exception`：异常记录

---

### 12. 异常记录表（t_exception）

**表说明**：异常主表，记录异常信息，通过多态关联支持关联多个业务实体

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| business_type | VARCHAR(50) | 是 | - | 业务类型（多态关联） |
| business_id | BIGINT | 是 | - | 业务ID（多态关联） |
| exception_type_id | BIGINT | 是 | - | 异常类型ID（外键） |
| exception_title | VARCHAR(200) | 是 | - | 异常标题 |
| exception_description | TEXT | 否 | NULL | 异常描述 |
| severity | VARCHAR(20) | 是 | 'MEDIUM' | 严重程度（HIGH/MEDIUM/LOW） |
| status | VARCHAR(30) | 是 | 'PENDING' | 处理状态 |
| reporter_id | BIGINT | 是 | 0 | 上报人ID |
| handler_id | BIGINT | 否 | 0 | 处理人ID |
| handled_time | DATETIME(3) | 否 | NULL | 处理时间 |
| handle_result | TEXT | 否 | NULL | 处理结果 |

**索引**：

```sql
PRIMARY KEY (`id`),
KEY `idx_exception_business` (`business_type`, `business_id`, `is_deleted`),
KEY `idx_exception_type` (`exception_type_id`, `is_deleted`),
KEY `idx_exception_status` (`status`, `is_deleted`)
```

**处理状态（status）**：
- `PENDING`：待处理
- `ASSIGNED`：已分配
- `PROCESSING`：处理中
- `RESOLVED`：已解决
- `CLOSED`：已关闭

---

### 13. 操作日志表（t_operation_log）

**表说明**：记录用户对业务实体的操作，用于审计追溯。采用**混合存储方案**：
- **核心信息**（结构化）→ MySQL 主表
- **详细快照**（大JSON）→ 对象存储（OSS/MinIO）

**存储策略**：
| 数据类型 | 存储方式 | 查询方式 |
|---------|---------|----------|
| 核心信息（操作人、时间、结果） | MySQL 主表 | 快速查询、统计分析 |
| 详细快照（before/after） | 对象存储 | 按需读取 |
| 全文检索（日志内容） | Elasticsearch（可选） | 全文搜索 |

**核心字段**：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 是 | AUTO_INCREMENT | 主键ID |
| operator_id | BIGINT | 是 | -1 | 操作人ID（-1表示系统） |
| operator_name | VARCHAR(50) | 是 | '' | 操作人姓名（冗余，便于展示） |
| business_type | VARCHAR(50) | 是 | - | 业务类型：order/order_line/shipment等 |
| business_id | BIGINT | 是 | - | 业务ID（多态关联） |
| business_no | VARCHAR(100) | 是 | '' | 业务实体编号（冗余） |
| operation_type | VARCHAR(30) | 是 | - | 操作类型：CREATE/UPDATE/DELETE等 |
| operation_module | VARCHAR(50) | 是 | - | 操作模块：ORDER/PARTNER/SHIPMENT等 |
| operation_desc | VARCHAR(500) | 是 | '' | 操作描述 |
| operation_result | VARCHAR(30) | 是 | 'SUCCESS' | 操作结果：SUCCESS/FAILED/PARTIAL |
| result_desc | VARCHAR(500) | 是 | '' | 操作结果描述 |
| operation_ip | VARCHAR(50) | 是 | '' | 操作IP地址 |
| request_path | VARCHAR(500) | 是 | '' | 请求路径 |
| request_method | VARCHAR(10) | 是 | '' | 请求方法：GET/POST/PUT/DELETE |
| operation_time | DATETIME(3) | 是 | CURRENT_TIMESTAMP(3) | 操作时间 |
| operation_duration | INT | 否 | NULL | 操作耗时（毫秒） |
| snapshot_key | VARCHAR(500) | 否 | NULL | 数据快照文件Key（对象存储） |
| extra_info | JSON | 否 | NULL | 扩展信息（设备、浏览器等） |
| is_deleted | TINYINT | 是 | 0 | 是否删除：0-未删除,1-已删除 |

**索引**：

```sql
PRIMARY KEY (`id`),

-- 操作人查询（高频场景：用户操作历史）
KEY `idx_operation_log_operator_time` (`operator_id`, `operation_time`, `is_deleted`),

-- 业务实体查询（高频场景：订单操作历史）
KEY `idx_operation_log_business_time` (`business_type`, `business_id`, `operation_time`, `is_deleted`),

-- 统计分析（按类型和模块统计）
KEY `idx_operation_log_type_module` (`operation_type`, `operation_module`, `operation_time`, `is_deleted`),

-- 时间范围查询（按时间查询和统计）
KEY `idx_operation_log_time_result` (`operation_time`, `operation_result`, `is_deleted`)
```

**混合存储说明**：

1. **MySQL 主表**：存储核心字段，支持快速查询和统计
2. **对象存储**：存储详细数据快照（`before_data`、`after_data`）
   - 文件路径：`logs/operation/{year}/{month}/{logId}.json`
   - 文件内容：`{"before": {...}, "after": {...}}`
   - 查询时通过 `snapshot_key` 字段获取对象

**操作类型（operation_type）**：
- `CREATE`：创建
- `UPDATE`：更新
- `DELETE`：删除
- `VIEW`：查看
- `EXPORT`：导出
- `IMPORT`：导入
- `AUDIT`：审核
- `CONFIRM`：确认
- `APPROVE`：审批
- `CANCEL`：取消

**操作模块（operation_module）**：
- `ORDER`：订单管理
- `PARTNER`：合作方管理
- `SHIPMENT`：发运管理
- `RECEIPT`：签收管理
- `ATTACHMENT`：附件管理
- `EXCEPTION`：异常管理
- `SYSTEM`：系统管理

---

## 索引设计

### 索引设计原则

1. **唯一索引**：用于保证唯一性约束（订单号、用户名等）
2. **联合索引**：用于多条件查询，遵循最左前缀原则
3. **覆盖索引**：包含查询所需的所有字段，避免回表
4. **软删除过滤**：联合索引中包含 `is_deleted` 字段
5. **避免过度索引**：只保留核心查询场景的索引

### 主要索引列表

#### 唯一索引

| 表名 | 索引名 | 字段 | 说明 |
|------|--------|------|------|
| t_order | uk_order_order_no | order_no, is_deleted | 订单号唯一 |
| t_user | uk_user_username | username, is_deleted | 用户名唯一 |
| t_customer | uk_customer_code | customer_code, is_deleted | 客户编码唯一 |
| t_supplier | uk_supplier_code | supplier_code, is_deleted | 供应商编码唯一 |
| t_carrier | uk_carrier_code | carrier_code, is_deleted | 承运商编码唯一 |

#### 联合索引

| 表名 | 索引名 | 字段 | 说明 |
|------|--------|------|------|
| t_order | idx_order_customer_status | customer_id, status_code, is_deleted | 客户状态查询 |
| t_order | idx_order_date_status | order_date, status_code, is_deleted | 订单日期状态查询 |
| t_order_line | idx_order_line_order_status | order_id, status_code, is_deleted | 订单行查询 |
| t_shipment_line | idx_shipment_line_tracking | tracking_no, is_deleted | 物流单号查询 |
| t_attachment | idx_attachment_business | business_type, business_id, is_deleted | 多态关联查询 |

### 索引使用建议

1. **查询优化**：优先使用索引字段进行查询
2. **排序优化**：索引字段可用于排序，避免文件排序
3. **覆盖查询**：尽量使用覆盖索引，减少回表操作
4. **索引监控**：定期监控索引使用情况，删除未使用的索引
5. **EXPLAIN分析**：使用EXPLAIN分析查询执行计划

---

## 开发规范

### SQL编写规范

#### 1. 避免SELECT *

```sql
-- ✅ 正确：明确指定需要的列
SELECT id, order_no, customer_id, total_amount
FROM t_order
WHERE is_deleted = 0 AND status_code = 'EXECUTING';

-- ❌ 错误：使用 SELECT *
SELECT * FROM t_order;
```

#### 2. 软删除过滤

```sql
-- ✅ 正确：所有查询必须过滤 is_deleted
SELECT * FROM t_order WHERE is_deleted = 0;

-- ❌ 错误：忘记过滤软删除数据
SELECT * FROM t_order;
```

#### 3. 批量操作

```java
// ✅ 正确：使用批量插入
List<Order> orders = ...;
orderService.saveBatch(orders);

// ❌ 错误：循环单个插入
for (Order order : orders) {
    orderService.save(order);
}
```

#### 4. 使用MyBatis Plus注解

```java
@Mapper
public interface OrderMapper extends BaseMapper<Order> {

    @Select("SELECT * FROM t_order " +
            "WHERE customer_id = #{customerId} " +
            "AND is_deleted = 0 " +
            "ORDER BY created_at DESC " +
            "LIMIT #{pageSize}")
    List<Order> selectByCustomerId(@Param("customerId") Long customerId,
                                     @Param("pageSize") int pageSize);
}
```

### 字段设计规范

#### NOT NULL 规范

**原则**：尽可能使用 `NOT NULL` 和合适的默认值，避免使用 NULL

**正确示例**：
```sql
-- 状态:默认空字符串表示初始创建状态,避免使用NULL防止索引失效
`status_code` VARCHAR(30) NOT NULL DEFAULT '' COMMENT '订单状态',

-- 创建人ID:默认0表示系统创建,避免使用NULL防止空指针问题
`created_by` BIGINT NOT NULL DEFAULT 0 COMMENT '创建人ID:0-系统创建',
```

#### 字段类型选择

| 用途 | 类型 | 说明 |
|------|------|------|
| 主键ID | BIGINT | 支持自增或雪花算法 |
| 外键ID | BIGINT | 关联其他表的主键 |
| 金额 | DECIMAL(18,2) | 精确计算 |
| 数量 | DECIMAL(18,2) | 支持小数 |
| 状态码 | VARCHAR(30) | 状态代码 |
| 布尔值 | TINYINT | 0-否，1-是 |
| 时间 | DATETIME(3) | 精确到毫秒 |
| JSON数据 | JSON | MySQL 5.7+ |

---

## 修改规范

> **🔔 重要**：数据库是系统的核心，修改时请严格遵守以下规范。

### 📝 修改前检查清单

- [ ] 确认修改是否影响现有数据
- [ ] 确认是否需要停机维护
- [ ] 确认是否需要数据迁移
- [ ] 确认是否需要更新应用代码
- [ ] 确认是否需要回滚方案
- [ ] **更新本文档的"更新记录"部分**

### ✅ 允许的修改

| 修改类型 | 说明 | 示例 |
|---------|------|------|
| **新增表** | 添加新的业务表 | 新增报表模块表 |
| **新增字段** | 在现有表中添加字段 | 订单表新增支付方式字段 |
| **新增索引** | 添加索引优化查询 | 订单表新增客户状态联合索引 |
| **修改字段长度** | 扩大字段长度 | VARCHAR(50) → VARCHAR(200) |
| **修改默认值** | 调整字段默认值 | 状态默认值调整 |
| **Bug 修复** | 修复数据库设计问题 | 修复索引错误 |
| **性能优化** | 优化数据库性能 | 添加分区、优化索引 |

### ❌ 禁止的修改

| 修改类型 | 原因 | 替代方案 |
|---------|------|----------|
| **删除表** | 会破坏系统功能 | 标记为 `is_deleted = 1` |
| **删除字段** | 会导致应用错误 | 标记为废弃，保留字段 |
| **缩小字段长度** | 可能导致数据截断 | 新增字段，迁移数据 |
| **修改主键** | 会破坏关联关系 | 不修改主键 |
| **修改外键** | 会破坏引用完整性 | 谨慎评估，确保无风险 |
| **修改字段类型（缩小）** | 可能导致数据丢失 | 扩大类型可以，缩小不可以 |

### 🔄 数据库版本管理

#### 使用 Flyway 进行版本控制

```sql
-- 命名规范：V{版本号}__{描述}.sql
-- 文件位置：resources/db/migration/

-- V1.0.0__init_database.sql
CREATE TABLE t_order (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(50) NOT NULL,
    ...
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- V1.0.1__add_order_index.sql
CREATE INDEX idx_order_customer_status ON t_order(customer_id, status_code);

-- V1.0.2__add_receipt_table.sql
CREATE TABLE t_receipt_detail (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    ...
);
```

#### 版本号规则

- **主版本号**：重大架构变更（如分库分表）
- **次版本号**：新增表或重要字段
- **修订号**：索引优化、Bug修复

示例：
- `V1.0.0`：初始版本
- `V1.0.1`：新增订单索引
- `V1.1.0`：新增签收表
- `V2.0.0`：分库分表改造

### 📋 修改流程

#### 1. 评估阶段
- 评估影响范围（涉及哪些表、哪些业务）
- 评估数据量（需要迁移多少数据）
- 评估执行时间（预计需要多久）
- 评估风险等级（高/中/低）

#### 2. 设计阶段
- 编写SQL脚本（Flyway migration）
- 编写回滚脚本（undo migration）
- 编写数据迁移脚本（如需要）
- 编写验证脚本（验证修改是否成功）

#### 3. 测试阶段
- 在测试环境执行SQL
- 验证数据完整性
- 验证应用功能
- 验证性能影响

#### 4. 执行阶段
- 备份生产数据库
- 在维护窗口执行
- 监控执行过程
- 验证执行结果

#### 5. 更新文档
- **必须更新本文档的"更新记录"部分**
- 更新相关表结构说明
- 更新索引设计说明

### 📋 修改模板

修改数据库时，请按以下模板更新本文档末尾的"更新记录"部分：

```markdown
### V{版本号} (YYYY-MM-DD)

#### {修改类型}

**修改人**：你的名字

**修改说明**：
- 修改背景和原因
- 修改内容和范围

**SQL变更**：
```sql
-- SQL变更内容
```

**表结构变更**：
| 表名 | 变更类型 | 说明 |
|------|----------|------|
| t_order | 新增字段 | 新增 payment_method 字段 |

**影响范围**：
- 影响的模块
- 需要修改的代码

**相关 Issue/PR**：#123
```

---

## 数据字典

### 业务类型（business_type）

用于多态关联和状态管理中的业务类型标识。

| 代码 | 说明 | 使用场景 |
|------|------|----------|
| order | 订单 | 订单表、状态字典、附件、异常 |
| order_line | 订单行 | 订单行表、状态字典、异常 |
| shipment | 发运计划 | 发运计划表、状态字典、附件 |
| shipment_line | 快递单 | 快递单表、状态字典、异常 |
| receipt_detail | 签收明细 | 签收明细表、异常 |
| supplier | 供应商 | 供应商表、附件 |
| carrier | 承运商 | 承运商表、附件 |
| customer | 客户 | 客户表 |
| exception | 异常 | 异常记录表 |

### 状态代码（status_code）

状态代码定义在 `t_status_dict` 表中，不同业务类型有不同的状态代码。

**订单状态**：
| 状态码 | 说明 | 是否终态 |
|--------|------|----------|
| DRAFT | 草稿 | 否 |
| CONFIRMED | 已确认 | 否 |
| EXECUTING | 执行中 | 否 |
| PARTIALLY_RECEIVED | 部分到货 | 否 |
| COMPLETED | 已完成 | 是 |
| CANCELLED | 已取消 | 是 |

**发运计划状态**：
| 状态码 | 说明 | 是否终态 |
|--------|------|----------|
| DRAFT | 草稿 | 否 |
| APPROVED | 已审核 | 否 |
| EXECUTING | 执行中 | 否 |
| COMPLETED | 已完成 | 是 |

**快递单状态**：
| 状态码 | 说明 | 是否终态 |
|--------|------|----------|
| CREATED | 已创建 | 否 |
| PICKED_UP | 已取货 | 否 |
| IN_TRANSIT | 运输中 | 否 |
| DELIVERED | 已送达 | 否 |
| RECEIVED | 已签收 | 是 |

**签收状态**：
| 状态码 | 说明 |
|--------|------|
| RECEIVED | 已签收（无差异） |
| DIFFERENCE | 有差异 |
| PROCESSED | 已处理 |

**异常处理状态**：
| 状态码 | 说明 |
|--------|------|
| PENDING | 待处理 |
| ASSIGNED | 已分配 |
| PROCESSING | 处理中 |
| RESOLVED | 已解决 |
| CLOSED | 已关闭 |

---

## 相关文档

- [数据库命名规范](../docs/数据库/1.数据库命名规范.md)
- [数据库评估报告](../docs/数据库/数据库评估报告.md)
- [API接口文档](./API接口文档.md)
- [系统架构设计](./系统架构设计.md)

---

## 维护者

- **开发组** - 数据库设计与文档维护

---

## 许可证

本项目采用 MIT 许可证。

---

## 更新记录

### V1.0.2 (2026-01-07)

#### 用户表设计优化

**修改人**：开发组

**修改背景**：
根据实际项目需求，完善用户表设计，增加账号安全、组织信息、用户统计等字段，避免后期频繁修改表结构。

**修改内容**：
- 扩展用户表字段从13个到25个
- 新增账号安全字段：`locked_time`、`locked_reason`、`login_count`、`password_changed_time`、`password_expire_time`
- 新增业务标识字段：`user_code`
- 新增组织信息字段：`department_id`、`department_name`、`position`、`employee_no`
- 新增部门索引：`idx_user_department`

**设计原则**：
- 考虑长期维护成本，避免频繁 ALTER TABLE
- 满足实际生产环境的安全审计需求
- 支持数据权限隔离（基于部门）
- 预留用户行为统计基础

---

### V1.0.3 (2026-01-07)

#### 新增用户权限模块核心表设计

**修改人**：开发组

**修改背景**：
根据甲方需求文档定义的三层权限控制体系（功能权限、数据权限、字段权限），设计用户权限模块的核心表结构。

**修改内容**：
- ✅ 新增"11. 角色表（t_role）"
  - 13字段设计（相比参考设计减少41%冗余）
  - data_scope_type 作为数据权限控制的核心字段
  - 5个预定义角色：SYSTEM_ADMIN、DATA_ADMIN、CUSTOMER_MANAGER、PURCHASE_SPECIALIST、OPERATION_SPECIALIST
  - is_system 标识系统内置角色，防止误删

- ✅ 新增"12. 用户角色关联表（t_user_role）"
  - 11字段设计（相比参考设计减少50%冗余）
  - is_primary 标识主角色，用于数据权限判断
  - 适度冗余：role_code、username 字段减少 JOIN 查询

- ✅ 新增"13. 角色权限关联表（t_role_permission）"
  - 10字段设计（相比参考设计减少44%冗余）
  - permission_code 直接存储权限代码，不使用 t_permission 表
  - 权限代码格式：{模块}:{操作}（如 ORDER:VIEW、SHIPMENT:CREATE）
  - 包含5个预定义角色的完整权限初始化数据

**设计决策**：
1. **不使用 t_permission 表**：权限是代码层面的硬编码，不需要动态管理
2. **data_scope_type 在角色表而非用户表**：数据权限是角色特性，不是用户特性
3. **用户可有多角色**：通过 is_primary 标识主角色，用于数据权限判断
4. **适度冗余**：关联表存储 role_code 和 username，提高查询性能

**三层权限控制体系**：
- **功能权限**：通过 t_role_permission 表控制用户能访问哪些模块
- **数据权限**：通过 t_role.data_scope_type 控制用户能访问哪些数据
  - 1-ALL：全部数据（系统管理员、数据管理员）
  - 2-DEPARTMENT：本部门数据（部门经理）
  - 3-SELF：本人数据（业务角色）
  - 4-CUSTOM：自定义范围（预留扩展）
- **字段权限**：预留扩展，暂未实现

**相关文件**：
- order-platform-user/src/main/resources/sql/role.sql（已创建）
- order-platform-user/src/main/resources/sql/user_role.sql（已创建）
- order-platform-user/src/main/resources/sql/role_permission.sql（已创建）
- order-platform-user/README.md（已更新）

**相关文档**：
- 可视化数字化管理平台解决方案v1217.md（甲方需求文档）

---

### V1.0.4 (2026-01-07)

#### 用户表SQL脚本迁移和User模块实体类创建

**修改人**：开发组

**修改背景**：
User表是业务实体，应该归属于user模块而非common模块。将user.sql移动到user模块，并创建对应的Java实体类、Mapper和辅助工具，实现更清晰的模块职责划分。

**修改内容**：

1. **移动user.sql**：
   - 从 `order-platform-common/src/main/resources/sql/user.sql`
   - 移动到 `order-platform-user/src/main/resources/sql/user.sql`
   - User表SQL脚本归属于user模块管理

2. **创建User实体类**：
   - 路径：`order-platform-user/src/main/java/com/order/platform/user/entity/User.java`
   - 25字段完整设计
   - 支持账号安全（isLocked、passwordExpireTime）
   - 支持登录统计（loginCount、lastLoginTime）
   - 支持组织信息（departmentId、position、employeeNo）
   - 使用MyBatis-Plus注解（@TableName、@TableId、@TableField等）

3. **创建UserMapper**：
   - 路径：`order-platform-user/src/main/java/com/order/platform/user/mapper/UserMapper.java`
   - 继承BaseMapper<User>，提供基础CRUD
   - 新增4个查询方法：
     - selectByUsername：根据用户名查询（登录验证）
     - selectByUserCode：根据用户编号查询
     - selectByEmail：根据邮箱查询
     - selectByPhone：根据手机号查询

4. **创建AuthHelper（认证辅助工具）**：
   - 路径：`order-platform-user/src/main/java/com/order/platform/user/service/AuthHelper.java`
   - 功能：User实体 → CurrentUser DTO转换
   - 解耦User模块与Common模块
   - 只提取Token中需要的11个核心字段

5. **修复UserRole实体类**：
   - 添加缺失字段：username、is_primary、createdAt、createdBy、updatedAt、updatedBy
   - 删除多余字段：isEnabled（表中不存在）
   - 使用@TableField注解实现自动填充

6. **修复UserRoleMapper**：
   - 移除SQL中不存在的is_enabled条件
   - 更新查询条件注释

**设计决策**：
1. **User表归属**：User是业务实体，归属于user模块而非common模块
2. **模块解耦**：Common模块只依赖CurrentUser DTO，不依赖User实体类
3. **转换层**：通过AuthHelper实现User → CurrentUser的转换
4. **职责清晰**：User模块负责用户业务，Common模块提供通用功能

**影响范围**：
- Common模块：零影响（完全解耦，不依赖User实体类）
- User模块：新增User实体、UserMapper、AuthHelper

**相关文件**：
- order-platform-user/src/main/resources/sql/user.sql（已移动）
- order-platform-user/src/main/java/com/order/platform/user/entity/User.java（已创建）
- order-platform-user/src/main/java/com/order/platform/user/entity/UserRole.java（已修复）
- order-platform-user/src/main/java/com/order/platform/user/mapper/UserMapper.java（已创建）
- order-platform-user/src/main/java/com/order/platform/user/mapper/UserRoleMapper.java（已修复）
- order-platform-user/src/main/java/com/order/platform/user/service/AuthHelper.java（已创建）
- order-platform-user/README.md（已更新）
- order-platform-common/README.md（已更新）

---

### V1.0.1 (2026-01-07)

#### 新增操作日志表设计

**修改人**：开发组

**修改内容**：
- 在"表结构设计"章节新增"13. 操作日志表（t_operation_log）"
- 采用混合存储方案：核心信息存 MySQL，详细快照存对象存储
- 精简字段设计（约20个核心字段），避免过度冗余
- 支持多态关联（business_type + business_id）
- 添加 snapshot_key 字段关联对象存储中的数据快照文件
- 定义操作类型和操作模块枚举值
- 设计4个联合索引覆盖高频查询场景

**相关文件**：
- 数据库设计文档.md（已更新）
- 系统架构设计.md（待更新 common 模块日志留痕说明）

---

### V1.0.0 (2026-01-07)

#### 初始数据库设计

**创建人**：开发组

**设计内容**：
- ✅ 订单模块（4张表）
- ✅ 发运模块（4张表）
- ✅ 签收模块（1张表）
- ✅ 合作方模块（4张表）
- ✅ 异常管理（3张表）
- ✅ 附件管理（3张表）
- ✅ 状态管理（2张表）
- ✅ 用户权限（5张表）
- ✅ 系统支撑（3张表）

**设计特点**：
- ✅ 统一的状态机管理
- ✅ 灵活的多态关联设计
- ✅ 完善的软删除机制
- ✅ 详细的字段注释
- ✅ NOT NULL 优先原则

**待优化项**：
- ⏳ 补充核心业务索引
- ⏳ 实现表分区策略
- ⏳ 配置数据库备份
- ⏳ 实现读写分离
